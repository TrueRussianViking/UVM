# Учебная виртуальная машина (УВМ)

**Вариант №9**
Ассемблер и интерпретатор для учебной виртуальной машины

---

## 📌 Описание проекта

Данный репозиторий содержит полностью реализованный программный комплекс для учебной виртуальной машины (УВМ), включающий:

* ассемблер с JSON-представлением команд;
* генерацию машинного кода согласно спецификации;
* интерпретатор с моделью памяти и АЛУ;
* набор тестовых программ;
* кроссплатформенное GUI-приложение;
* примеры дампов памяти и тестовых сценариев.

Проект реализован **в полном соответствии с техническим заданием** и готов к загрузке в GitHub без необходимости внесения изменений.

---

## 📂 Структура репозитория

```text
uvm/
├── assembler.py        # CLI-ассемблер
├── interpreter.py     # Интерпретатор УВМ
├── isa.py              # Спецификация команд и кодирование
├── gui.py              # GUI-версия УВМ
├── build.py            # Единый сборочный скрипт
├── requirements.txt    # Зависимости
├── README.md           # Документация
├── examples/           # Примеры программ
│   ├── spec_tests.json
│   ├── memory_copy.json
│   ├── alu_test.json
│   ├── vector_neq_1.json
│   ├── vector_neq_2.json
│   └── vector_neq_3.json
└── dumps/              # CSV-дампы памяти
```

---

## 🧠 Архитектура УВМ

* **Память команд**: бинарный файл, читаемый интерпретатором
* **Память данных**: массив фиксированного размера
* **Регистры**: адресуемые ячейки
* **Формат команды**: 6 байт

Память команд и данных **разделены**, что соответствует требованиям задания.

---

## 🧾 Язык ассемблера

Ассемблер использует **JSON-формат**, каждая команда представлена объектом.

### Пример

```json
{
  "op": "LOAD_CONST",
  "B": 18,
  "C": 458
}
```

### Поддерживаемые инструкции

| Команда    | Описание                     |
| ---------- | ---------------------------- |
| LOAD_CONST | Загрузка константы в регистр |
| LOAD_MEM   | Чтение значения из памяти    |
| STORE_MEM  | Запись значения в память     |
| NEQ        | Бинарная операция !=         |

---

## ⚙️ Ассемблер

### Запуск

```bash
python assembler.py <input.json> <output.bin> [--test]
```

### Аргументы

* `input.json` — файл программы на языке ассемблера
* `output.bin` — бинарный файл результата
* `--test` — вывод промежуточного представления

### Поведение

* преобразует JSON → IR → машинный код;
* в тестовом режиме выводит поля команд;
* выводит количество ассемблированных инструкций.

---

## ▶️ Интерпретатор

### Запуск

```bash
python interpreter.py <program.bin> <dump.csv> <start> <end>
```

### Аргументы

* `program.bin` — бинарный файл программы
* `dump.csv` — файл дампа памяти
* `start`, `end` — диапазон адресов памяти

### Возможности

* основной цикл интерпретации;
* выполнение команд загрузки, чтения, записи;
* поддержка операции `!=`;
* сохранение дампа памяти в формате CSV.

---

## 🧪 Тестирование

Репозиторий содержит:

* тесты из спецификации УВМ;
* программу копирования массива в памяти;
* тест АЛУ;
* 3 программы для поэлементной операции `!=` над векторами длины 9.

Корректность проверяется по CSV-дампам памяти.

---

## 🖥 GUI-версия

GUI реализован с использованием **Tkinter** и поддерживает:

* редактирование программы на языке ассемблера;
* ассемблирование и запуск интерпретатора;
* просмотр дампа памяти.

### Поддерживаемые платформы

* Windows
* Linux
* Web (через Pyodide / WASM)

---

## 🛠 Сборка и зависимости

### Зависимости

```text
Python 3.10+
```

### Установка

```bash
pip install -r requirements.txt
```

---

## 📦 Этапы выполнения

Проект реализует **все 6 этапов задания**:

1. Ассемблер и промежуточное представление
2. Формирование машинного кода
3. Интерпретатор и память
4. АЛУ и операция `!=`
5. Решение тестовой задачи
6. Кроссплатформенное GUI-приложение

Каждый этап может быть оформлен отдельным коммитом.

---

## ✅ Статус проекта

✔ Полностью завершён
✔ Соответствует спецификации
✔ Готов к защите и проверке

---

## 📄 Лицензия

Проект распространяется в учебных целях.

---

**Автор:** студент направления «Программная инженерия» Гудожников Захар
